overwrite: true
schema: "http://localhost:4000" # Apollo Serverのエンドポイント
documents: "src/**/*.tsx" # GraphQL クエリを含むファイルのパス
generates:
  src/generated/graphql.tsx:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-react-apollo"
